package chapter4;

public class E6 {
	/* LAMBDA EXPRESSIONS */
	// An anonymous method, so to speak. You can pass it as if it were a variable (pass by value).
	
	// DEFERRED EXECUTION: code is specified now, but is run later.
	
	public static void main(String[] args) {
		
		// This is a lambda expression.
		// Three parts : I. single parameter, II. arrow operator, III. method call and result.
		
		//System.out.println(() -> true);
	}
}
